# Copie ce fichier en .streamlit/secrets.toml (en local) et remplis ta vraie clé.
# Ce fichier .example est sûr à committer ; secrets.toml lui-même ne doit JAMAIS être commité
# (il est déjà exclu par le .gitignore).

TMDB_API_KEY = "ta_cle_api_tmdb_ici"
